{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "service-express debugger attach",
      "type": "node",
      "request": "attach",
      "port": 10000,
      "restart": true,
      "skipFiles": ["<node_internals>/**"],
      "cwd": "${workspaceFolder}"
    }
  ]
}